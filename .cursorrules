# KidsMaster 项目规则

## 接口对接

- **对接或修改后端接口时**：必须先阅读项目中的接口文档，再编写或修改 `utils/api.uts` 及调用处。
- **接口文档位置**（按优先级）：
  1. `README.md` 中的「接口文档」
  2. 用户对话中提供的接口说明
- **操作顺序**：先读取上述文档或用户提供的接口定义 → 确认请求方法、路径、请求体/响应体、字段含义 → 再实现或修改 `utils/api.uts` 及页面调用逻辑。
