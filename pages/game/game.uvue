<template>
	<view class="container">
		<view class="header">
			<text class="title">🎮 游戏</text>
		</view>
		<scroll-view class="game-list" scroll-y="true">
			<view class="game-row" v-for="(row, rowIndex) in gameRows" :key="rowIndex">
				<view 
					class="game-card" 
					v-for="(game, index) in row" 
					:key="index"
					@click="goToGameDetail(game)"
				>
					<view class="card-cover">
						<text class="cover-emoji">{{ game.emoji }}</text>
					</view>
					<text class="card-title">{{ game.name }}</text>
				</view>
			</view>
		</scroll-view>
	</view>
</template>

<script lang="uts">
	type GameItem = {
		id: any
		name: string
		emoji: string
		description: string
	}
	
	export default {
		data() {
			const games: GameItem[] = [
				{ id: 1, name: "舒尔特方格", emoji: "🧩", description: "快速寻找到数字，提升专注能力" },
				{ id: "2", name: "听数挑战", emoji: "🎧", description: "听数字并正确输入，提升听觉记忆" },
				{ id: "3", name: "冥想游戏", emoji: "🔤", description: "在冒险中学习字母，寓教于乐" },
				{ id: "4", name: "形状识别", emoji: "⭐", description: "认识各种形状，开发空间想象力" },
				{ id: "5", name: "记忆挑战", emoji: "🧠", description: "挑战记忆力，锻炼大脑" },
				{ id: "6", name: "音乐节奏", emoji: "🎵", description: "跟随节奏，培养音乐感" }
			]
			const rows: GameItem[][] = []
			for (let i = 0; i < games.length; i += 2) {
				rows.push(games.slice(i, i + 2))
			}
			return {
				games: games,
				gameRows: rows
			}
		},
		onLoad() {
			
		},
		methods: {
			goToGameDetail(game: GameItem) {
				// 根据游戏ID跳转到对应的详情页面
				const gameId = game.id
				if (gameId === 1 || gameId === "1" || game.name === "舒尔特方格" || game.name.includes("舒尔特")) {
					// 舒尔特方格 - 跳转到专属详情页
					uni.navigateTo({
						url: '/pages/game/detail/schulte-detail'
					})
				} else if (gameId === "2" || game.name === "听数挑战" || game.name.includes("听数")) {
					// 听数挑战 - 跳转到专属详情页
					uni.navigateTo({
						url: '/pages/game/detail/numhearing-detail'
					})
				} else if (gameId === "3" || game.name === "冥想游戏" || game.name.includes("冥想")) {
					// 冥想游戏 - 跳转到专属详情页
					uni.navigateTo({
						url: '/pages/game/detail/meditation-detail'
					})
				} else if (gameId === "4" || game.name === "形状识别" || game.name.includes("形状")) {
					// 形状识别 - 跳转到专属详情页
					uni.navigateTo({
						url: '/pages/game/detail/shape-detail'
					})
				} else if (gameId === "5" || game.name === "记忆挑战" || game.name.includes("记忆")) {
					// 记忆挑战 - 跳转到专属详情页
					uni.navigateTo({
						url: '/pages/game/detail/memory-detail'
					})
				} else if (gameId === "6" || game.name === "音乐节奏" || game.name.includes("音乐")) {
					// 音乐节奏 - 跳转到专属详情页
					uni.navigateTo({
						url: '/pages/game/detail/music-detail'
					})
				} else {
					// 其他游戏跳转到通用详情页面
					uni.navigateTo({
						url: `/pages/game/detail/detail?id=${game.id}&name=${encodeURIComponent(game.name)}&emoji=${encodeURIComponent(game.emoji)}&description=${encodeURIComponent(game.description)}`
					})
				}
			}
		}
	}
</script>

<style>
	.container {
		flex: 1;
		background: linear-gradient(135deg, #A8E6CF 0%, #D4F1F4 100%);
		padding: 20px;
	}
	
	.header {
		margin-top: 40px;
		margin-bottom: 30px;
	}
	
	.title {
		font-size: 32px;
		font-weight: bold;
		color: #4ECDC4;
		text-align: center;
		text-shadow: 2px 2px 0px #FFFFFF, 4px 4px 0px rgba(0,0,0,0.1);
	}
	
	.game-list {
		flex: 1;
		margin-top: 20px;
	}
	
	.game-row {
		flex-direction: row;
		justify-content: space-between;
		margin-bottom: 20px;
		padding: 0 5px;
	}
	
	.game-card {
		width: 48%;
		background: rgba(255, 255, 255, 0.9);
		border-radius: 20px;
		border: 4px solid #FFFFFF;
		box-shadow: 0px 6px 0px rgba(0,0,0,0.15), 0px 3px 0px rgba(0,0,0,0.1);
		padding: 15px;
		align-items: center;
	}
	
	.game-card:active {
		transform: translateY(2px);
		box-shadow: 0px 3px 0px rgba(0,0,0,0.1), 0px 1px 0px rgba(0,0,0,0.05);
	}
	
	.card-cover {
		width: 100%;
		height: 140px;
		background: linear-gradient(135deg, #FFE5B4 0%, #FFCCCB 100%);
		border-radius: 15px;
		border: 3px solid #FFFFFF;
		align-items: center;
		justify-content: center;
		margin-bottom: 12px;
	}
	
	.cover-emoji {
		font-size: 70px;
	}
	
	.card-title {
		font-size: 18px;
		font-weight: bold;
		color: #4ECDC4;
		text-align: center;
		text-shadow: 1px 1px 0px #FFFFFF;
	}
</style>
